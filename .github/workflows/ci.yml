# ──────────────────────────────────────────────────────────────
# Reusable CI/CD workflow
#
# Usage (caller .github/workflows/ci.yml):
#   jobs:
#     ci:
#       uses: s1line1/actions-workflows/.github/workflows/workflow.yml@main
#       with:
#         acr-name: mycompany.azurecr.io
#         image-name: myapp
#       secrets:
#         ACR_USERNAME: ${{ secrets.ACR_USERNAME }}
#         ACR_PASSWORD: ${{ secrets.ACR_PASSWORD }}
# ──────────────────────────────────────────────────────────────

name: Shared CI

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        default: '20'
      acr-name:
        type: string
        default: ''
      image-name:
        type: string
        default: 'app'
      dockerfile:
        type: string
        default: 'Dockerfile'
      codeql-languages:
        type: string
        default: 'javascript'
      eslint-dir:
        type: string
        default: 'src'
      fail-on-cvss:
        type: number
        default: 7
      runs-on:
        type: string
        default: 'ubuntu-latest'

    secrets:
      ACR_USERNAME:
        required: false
      ACR_PASSWORD:
        required: false

    outputs:
      image-tag:
        description: Docker image tag (<run_number>-<short_sha>)
        value: ${{ jobs.build.outputs.image-tag }}

  workflow_dispatch:
    inputs:
      node-version:
        type: string
        default: '20'
      acr-name:
        type: string
        default: ''
      image-name:
        type: string
        default: 'app'
      dockerfile:
        type: string
        default: 'Dockerfile'
      codeql-languages:
        type: string
        default: 'javascript'
      eslint-dir:
        type: string
        default: 'src'
      fail-on-cvss:
        type: number
        default: 7
      runs-on:
        type: string
        default: 'ubuntu-latest'

permissions:
  contents: read

jobs:
  # ══════════════════════════════════════════════════════════
  # Lint
  # ══════════════════════════════════════════════════════════
  lint:
    name: Lint & Format
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: ${{ inputs.node-version || github.event.inputs.node-version || '20' }}

      - run: npm ci
        if: hashFiles('package-lock.json') != ''

      - run: npx eslint ${{ inputs.eslint-dir || github.event.inputs.eslint-dir || 'src' }}/ --max-warnings=0
        if: hashFiles('package.json') != ''

      - run: npx prettier --check ${{ inputs.eslint-dir || github.event.inputs.eslint-dir || 'src' }}/
        if: hashFiles('package.json') != ''
        continue-on-error: true

  # ══════════════════════════════════════════════════════════
  # SAST — CodeQL
  # ══════════════════════════════════════════════════════════
  sast:
    name: CodeQL
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - uses: github/codeql-action/init@v3
        with:
          languages: ${{ inputs.codeql-languages || github.event.inputs.codeql-languages || 'javascript' }}
      - uses: github/codeql-action/analyze@v3

  # ══════════════════════════════════════════════════════════
  # Dependency Scan — OWASP Dependency Check
  # ══════════════════════════════════════════════════════════
  dependency-scan:
    name: Dependency Scan
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    permissions:
      security-events: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: ${{ inputs.node-version || github.event.inputs.node-version || '20' }}

      - run: npm ci
        if: hashFiles('package-lock.json') != ''

      - uses: dependency-check/Dependency-Check_Action@v1
        continue-on-error: true
        with:
          project: ${{ github.repository }}
          path: .
          format: SARIF
          failOnCVSS: ${{ inputs.fail-on-cvss || github.event.inputs.fail-on-cvss || 7 }}

      - uses: github/codeql-action/upload-sarif@v3
        continue-on-error: true
        with:
          sarif_file: reports/dependency-check-report.sarif

  # ══════════════════════════════════════════════════════════
  # Secret Scan — GitLeaks
  # ══════════════════════════════════════════════════════════
  secret-scan:
    name: Secret Scan
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        continue-on-error: true

  # ══════════════════════════════════════════════════════════
  # Build — compile + test + Docker image
  # ══════════════════════════════════════════════════════════
  build:
    name: Build & Test
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    needs: [lint, sast, dependency-scan, secret-scan]
    outputs:
      image-tag: ${{ steps.tag.outputs.image-tag }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        if: hashFiles('package.json') != ''
        with:
          node-version: ${{ inputs.node-version || github.event.inputs.node-version || '20' }}

      - run: npm ci
        if: hashFiles('package-lock.json') != ''
      - run: npm run build
        if: hashFiles('package.json') != ''
      - run: npm test
        if: hashFiles('package.json') != ''

      - name: Compute image tag
        id: tag
        run: |
          SHORT_SHA="$(echo '${{ github.sha }}' | cut -c1-7)"
          echo "image-tag=${{ github.run_number }}-${SHORT_SHA}" >> "$GITHUB_OUTPUT"

      - name: Docker build
        run: |
          docker build \
            --label "git.commit=${{ github.sha }}" \
            --label "git.branch=${{ github.ref_name }}" \
            --label "build.run=${{ github.run_id }}" \
            -f ${{ inputs.dockerfile || github.event.inputs.dockerfile || 'Dockerfile' }} \
            -t ${{ inputs.image-name || github.event.inputs.image-name || 'app' }}:${{ steps.tag.outputs.image-tag }} \
            .

  # ══════════════════════════════════════════════════════════
  # Push to ACR (only when acr-name is configured)
  # ══════════════════════════════════════════════════════════
  push-acr:
    name: Push to ACR
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    needs: build
    if: (inputs.acr-name || github.event.inputs.acr-name || '') != ''
    steps:
      - uses: actions/checkout@v4

      - uses: azure/docker-login@v1
        with:
          login-server: ${{ inputs.acr-name || github.event.inputs.acr-name || '' }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: Docker build & push
        run: |
          FULL_REF="${{ inputs.acr-name || github.event.inputs.acr-name || '' }}/${{ inputs.image-name || github.event.inputs.image-name || 'app' }}:${{ needs.build.outputs.image-tag }}"
          docker build \
            --label "git.commit=${{ github.sha }}" \
            --label "git.branch=${{ github.ref_name }}" \
            --label "build.run=${{ github.run_id }}" \
            -f ${{ inputs.dockerfile || github.event.inputs.dockerfile || 'Dockerfile' }} \
            -t "$FULL_REF" \
            .
          docker push "$FULL_REF"

      - name: Trivy scan
        uses: aquasecurity/trivy-action@master
        continue-on-error: true
        with:
          image-ref: ${{ inputs.acr-name || github.event.inputs.acr-name || '' }}/${{ inputs.image-name || github.event.inputs.image-name || 'app' }}:${{ needs.build.outputs.image-tag }}
          format: sarif
          output: trivy-results.sarif
          severity: HIGH,CRITICAL
          exit-code: 1

      - uses: github/codeql-action/upload-sarif@v3
        continue-on-error: true
        with:
          sarif_file: trivy-results.sarif

  # ══════════════════════════════════════════════════════════
  # Tag Release — stamp git tag after successful push
  # ══════════════════════════════════════════════════════════
  tag-release:
    name: Tag Release
    runs-on: ${{ inputs.runs-on || github.event.inputs.runs-on || 'ubuntu-latest' }}
    needs: push-acr
    if: (inputs.acr-name || github.event.inputs.acr-name || '') != ''
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - run: |
          set -euo pipefail
          VERSION=$(cat VERSION 2>/dev/null || echo "0.1.0")
          TAG="v${VERSION}+build.${{ github.run_id }}"
          git tag -a "$TAG" -m "Release $TAG — ${{ github.sha }}"
          git push origin "$TAG"
